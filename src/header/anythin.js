
var Hand = () => {
    return(
        <div>
            <p>here is the first line bitchooo<br/>
            yes thats the second line too
            </p>
        
        </div>
                    
    )
}
export {Hand}

/* if you want to export only put in braces {} both in exporting and importing,
 if u want to export default wiether that func or class without {} 
 
 in all cases note that in case of arrow func u have to put it as var const or let
 in order it to export properly.
 ARROW FUNC USE VAR/CONST/LET AND EXPORT {SMTHIN} THEN IMPORT {SMTHIN} */